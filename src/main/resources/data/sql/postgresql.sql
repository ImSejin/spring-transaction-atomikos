/*
 PostgreSQL 15.0, compiled by Visual C++ build 1914, 64-bit
 */
DROP TABLE IF EXISTS GOODS CASCADE;
CREATE TABLE GOODS
(
    GOODS_NO VARCHAR(14) PRIMARY KEY,
    GOODS_NM VARCHAR(100) NOT NULL
);

INSERT INTO GOODS (GOODS_NO, GOODS_NM)
VALUES ('8000380205486', '로아커 웨하스 콰드라티니 피넛버터 125g');
INSERT INTO GOODS (GOODS_NO, GOODS_NM)
VALUES ('8801068407044', 'samlip 삼립호빵 크림치즈고구마 270g (90g x 3개입)');

----------------------------------------------------------------------------------------------------

DROP TABLE IF EXISTS GOODS_ORDER CASCADE;
CREATE TABLE GOODS_ORDER
(
    ORDER_CD VARCHAR(36) NOT NULL,
    GOODS_NO VARCHAR(14) NOT NULL,
    CONSTRAINT PRIMARY KEY (ORDER_CD),
    CONSTRAINT GOODS_ORDER_GOODS_GOODS_NO_FK
        FOREIGN KEY (GOODS_NO) REFERENCES GOODS
);

INSERT INTO GOODS_ORDER (ORDER_CD, GOODS_NO)
VALUES ('0be71f3d-917f-4c48-94bd-d54ce86b9968', '8000380205486');
INSERT INTO GOODS_ORDER (ORDER_CD, GOODS_NO)
VALUES ('bfa44346-f033-4b99-9036-dacbacee7ec9', '8801068407044');
